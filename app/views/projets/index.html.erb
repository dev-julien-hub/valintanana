        <div class = "contain-chearch">
            <div class="outerContent flex-content1">
                <div class="_col-flex2">
                    <div class="categorie-text text-el">
                        Categories
                    </div>
                    <div class="tous-option">
                        <select name="categories" id="categorie" class="categories-class">
                            <option value="default"> Toutes</option>
							for
                                <option value="default"> Option </option>
                        </select>
                    </div>
                </div>

                <div class="_col-flex2">
                    <div class="text-el">
                        ODD
                    </div>
                        <div class="tous-option">
                            <select name="odd" id="odd" class="categories-class">
                                <option value="default"> Toutes</option>
                                @foreach ($data['odd'] as $rec)
                                    <option value="default"> {{ $rec->odd }}</option>
                                @endforeach
                            </select>
                        </div>
                    </div>

                <div class="_col-flex2">
                    <div class="text-el">
                       Status du projet
                    </div>
                        <div class="tous-option">
                            <select name="odd" id="odd" class="categories-class">
                                <option value="default"> Toutes</option>
                                <option value="default">Projet en cours</option>
                                <option value="default">Projet réussi </option>
                            </select>
                        </div>
                    </div>


                <div class="_col-flex2">
                    <div class="text-el">
                        Lieu d éxécution
                    </div>
                        <div class="tous-option">
                            <select name="odd" id="odd" class="categories-class">
                                <option value="default"> Toutes</option>
                                @foreach ($data['region'] as $rec)
                                    <option value="default"> {{ $rec->region }}</option>
                                @endforeach
                            </select>
                        </div>
                    </div>
                </div>
            </div>
<div class="pd-in">
	<div class="row">
	  	<% for @i in 1..25 %>
	  		<%=render 'layouts/projects' %>
	 	<% end %>
	</div>
</div>
